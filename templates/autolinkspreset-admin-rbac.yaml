apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "chart.fullname" . }}-autolinkspreset-admin-role
  labels:
  {{- include "chart.labels" . | nindent 4 }}
rules:
- apiGroups:
  - github.interhyp.de
  resources:
  - autolinkspresets
  verbs:
  - '*'
- apiGroups:
  - github.interhyp.de
  resources:
  - autolinkspresets/status
  verbs:
  - get
