apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "chart.fullname" . }}-team-admin-role
  labels:
  {{- include "chart.labels" . | nindent 4 }}
rules:
- apiGroups:
  - github.interhyp.de
  resources:
  - teams
  verbs:
  - '*'
- apiGroups:
  - github.interhyp.de
  resources:
  - teams/status
  verbs:
  - get
