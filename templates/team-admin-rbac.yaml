apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "git-hubby.fullname" . }}-team-admin-role
  labels:
  {{- include "git-hubby.labels" . | nindent 4 }}
rules:
- apiGroups:
  - github.interhyp.de
  resources:
  - teams
  verbs:
  - '*'
- apiGroups:
  - github.interhyp.de
  resources:
  - teams/status
  verbs:
  - get
