{{- with .Values.controllerManager.podDisruptionBudget }}
{{- if .enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "git-hubby.fullname" $ }}-controller-manager
  labels:
    control-plane: controller-manager
  {{- include "git-hubby.labels" $ | nindent 4 }}
spec:
  {{- with .minAvailable }}
  minAvailable: {{ . }}
  {{- end }}
  {{- with .maxUnavailable }}
  maxUnavailable: {{ . }}
  {{- end }}
  selector:
    matchLabels:
      control-plane: controller-manager
    {{- include "git-hubby.selectorLabels" $ | nindent 6 }}
{{- end }}
{{- end }}

