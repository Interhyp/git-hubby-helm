apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "git-hubby.fullname" . }}-webhookignorepreset-admin-role
  labels:
  {{- include "git-hubby.labels" . | nindent 4 }}
rules:
- apiGroups:
  - github.interhyp.de
  resources:
  - webhookignorepresets
  verbs:
  - '*'
- apiGroups:
  - github.interhyp.de
  resources:
  - webhookignorepresets/status
  verbs:
  - get
