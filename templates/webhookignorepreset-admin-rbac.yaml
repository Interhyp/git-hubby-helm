apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "chart.fullname" . }}-webhookignorepreset-admin-role
  labels:
  {{- include "chart.labels" . | nindent 4 }}
rules:
- apiGroups:
  - github.interhyp.de
  resources:
  - webhookignorepresets
  verbs:
  - '*'
- apiGroups:
  - github.interhyp.de
  resources:
  - webhookignorepresets/status
  verbs:
  - get
