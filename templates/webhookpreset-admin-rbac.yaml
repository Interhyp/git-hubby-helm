apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "chart.fullname" . }}-webhookpreset-admin-role
  labels:
  {{- include "chart.labels" . | nindent 4 }}
rules:
- apiGroups:
  - github.interhyp.de
  resources:
  - webhookpresets
  verbs:
  - '*'
- apiGroups:
  - github.interhyp.de
  resources:
  - webhookpresets/status
  verbs:
  - get
