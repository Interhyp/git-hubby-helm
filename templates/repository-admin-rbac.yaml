apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "chart.fullname" . }}-repository-admin-role
  labels:
  {{- include "chart.labels" . | nindent 4 }}
rules:
- apiGroups:
  - github.interhyp.de
  resources:
  - repositories
  verbs:
  - '*'
- apiGroups:
  - github.interhyp.de
  resources:
  - repositories/status
  verbs:
  - get
